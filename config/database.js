// Database configuration
// Currently using in-memory storage, but can be extended for actual databases

export const dbConfig = {
  // Add database configuration here when needed
  // For now, we're using in-memory storage and external APIs
};

export default dbConfig;
